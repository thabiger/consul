```release-note:improvement
dns: Added DNS locality-aware lookup via `dns_config.locality_aware_lookup`. Operators must provision Consul locality on agents and service instances. This option only affects in-agent DNS response candidate selection and does not change Connect, xDS, service mesh, or Enterprise locality-aware routing behavior.
```
